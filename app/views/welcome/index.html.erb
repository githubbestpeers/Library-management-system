<h1>Queries</h1>
 
<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Query</th>
  </tr>
 
  <% @welcomes.each do |welcome| %>
    <tr>
      <td><%= welcome.name %></td>
      <td><%= welcome.email %></td>
      <td><%= welcome.query %></td>
      <td><%#= link_to 'Show', article_path(article) %></td>
    </tr>
  <% end %>
</table>